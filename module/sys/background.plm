# $Id$
# copyright (c)1997-2000 Yoshinori Hasegawa <hasegawa@madoka.org>

package sys_background;

$_ = 'sys_background';

sub initialize {
  $PID = 'plum.pid';
  exit if fork();
  if (open(PID, ">$PID")) {
    print PID $$, "\n";
    close(PID);
  }
}

